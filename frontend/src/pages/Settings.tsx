import React from 'react';
import { Settings as SettingsIcon, Bell, Shield, Palette } from 'lucide-react';
export const Settings: React.FC = () => <div className="space-y-6"><div><h1 className="flex items-center gap-2 text-2xl font-bold text-slate-900"><SettingsIcon className="h-6 w-6 text-indigo-600" /> Settings</h1><p className="mt-1 text-sm text-slate-500">Configure HR portal defaults and notification preferences.</p></div><div className="grid grid-cols-1 gap-4 lg:grid-cols-3">{[[Bell, 'Notifications', 'Manage leave alerts, attendance reminders, and payroll updates.'], [Shield, 'Access Control', 'Review permissions for employee and HR workspaces.'], [Palette, 'Brand and Theme', 'Dayflow visual settings and product identity.']].map(([Icon, title, description]) => <div key={title as string} className="panel-elevated rounded-xl p-5"><div className="mb-3 inline-flex rounded-lg bg-indigo-50 p-2 text-indigo-600">{React.createElement(Icon as React.ElementType, { className: 'h-4 w-4' })}</div><h3 className="text-sm font-semibold text-slate-900">{title as string}</h3><p className="mt-1 text-xs text-slate-500">{description as string}</p><button className="mt-4 rounded-lg border border-slate-200 px-3 py-1.5 text-xs text-slate-600">Configure</button></div>)}</div></div>;
